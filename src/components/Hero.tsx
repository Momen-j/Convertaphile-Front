const Hero = () => {

  return (
    <>
      <div className="flex-col justify-center text-center text-app-text">
        <div className="text-7xl font-extrabold mb-1">Free File Conversion!</div>
        <div className={'text-3xl'}>Convert your files with zero hassle, zero cost</div>
      {/*<button onClick = {() => setHeroTitle((prevState) => !prevState)} className="text-indigo-500 mb-5 hover:text-red-500 rounded-lg bg-green-400">{herotitle ? "Hero Title True": "Hero Title False"}</button>*/}
      </div>
    </>
  );
};

export default Hero;
